{
  "name": "rs3-spinner",
  "version": "1.0.0",
  "private": true,
  "description": "DoubleShines Task Tracker (RS3 Spinner) - static site",
  "scripts": {
    "dev:css": "tailwindcss -i ./src/styles/tailwind.css -o ./assets/tailwind.css --watch",
    "build:css": "tailwindcss -i ./src/styles/tailwind.css -o ./assets/tailwind.css --minify",
    "build": "npm run build:css"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.10"
  }
}
